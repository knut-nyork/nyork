import * as React from 'react';
export interface PlaceholderProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const SitePlaceholder: React.FC<PlaceholderProps>;
export default SitePlaceholder;
